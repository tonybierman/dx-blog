# Generated by Cargo
# will have compiled files and executables
/target
.DS_Store

# These are backup files generated by rustfmt
**/*.rs.bk

# Local dev SQLite DB and uploaded media
/data
/uploads

# Local env
.env

# Personal, untracked just recipes (optionally imported by the justfile)
local.just

# Shipped image artifact produced by the deploy recipes (docker save | gzip)
/dx-blog.tar.gz

# Per-host compose deltas (proxy network, etc.) live only on the server.
docker-compose.override.yml

# Local planning notes
TODO.md
TODO*.md
NOTES.md

assets/tailwind.css
